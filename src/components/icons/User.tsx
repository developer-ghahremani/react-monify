import { AiOutlineUser } from "react-icons/ai";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const UserIcon = (props: Props) => <AiOutlineUser {...props} />;

export default UserIcon;
