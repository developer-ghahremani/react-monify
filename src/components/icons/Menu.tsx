import { FiMenu } from "react-icons/fi";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const MenuIcon = (props: Props) => <FiMenu {...props} />;

export default MenuIcon;
