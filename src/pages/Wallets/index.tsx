import { MainLayout } from "components/layout";
import React from "react";

type Props = {};

const Wallets = (props: Props) => {
  return <MainLayout>Wallets</MainLayout>;
};

export default Wallets;
