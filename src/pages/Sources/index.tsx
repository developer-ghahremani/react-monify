import { MainLayout } from "components/layout";
import React from "react";

type Props = {};

const Sources = (props: Props) => {
  return <MainLayout>Sources</MainLayout>;
};

export default Sources;
